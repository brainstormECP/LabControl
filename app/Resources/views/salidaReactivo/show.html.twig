{% extends 'layout.html.twig' %}

{% block content -%}
    <h1>Salida de Reactivo</h1>

    <table class="record_properties">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.Id }}</td>
        </tr>
        <tr>
            <th>Fecha</th>
            <td>{{ entity.Fecha|date('d/m/Y') }}</td>
        </tr>
        <tr>
            <th>Reactivo</th>
            <td>{{ entity.Reactivo }}</td>
        </tr>
        <tr>
            <th>Cantidad</th>
            <td>{{ entity.Cantidad }}</td>
        </tr>
        </tbody>
    </table>
    <div class="form-group">
        <a class="btn btn-default" href="{{ path('salidareactivo') }}">Lista de Salidas de Reactivo</a>
        {{ form(delete_form) }}
    </div>
{% endblock %}


