{% extends '::base.html.twig' %}

{% block body -%}
    <h1>AnalisisMuestra</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Aprobado</th>
                <td>{{ entity.aprobado }}</td>
            </tr>
            <tr>
                <th>Resultado</th>
                <td>{{ entity.resultado }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('analisismuestra') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('analisismuestra_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
