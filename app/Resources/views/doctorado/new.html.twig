{% extends 'layout.html.twig' %}

{% block content -%}
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading"> <h3>Agregar Doctorado</h3></div>
            <div class="panel-body">
                <div class="row">
                    <div class="col-lg-9">
                        {{ form_start(form, {'attr':{ 'class':'form-horizontal' }}) }}
                        <div class="form-group">
                            <div class="col-md-7">
                                {{ form_label(form.especialidad) }}
                                {{ form_widget(form.especialidad,{'attr':{ 'class':'form-control' }}) }}
                                {{ form_errors(form.especialidad) }}
                            </div>
                        </div>
                        <div class="form-group">
                            <div class="col-md-7">
                                {{ form_label(form.nombre) }}
                                {{ form_widget(form.nombre,{'attr':{ 'class':'form-control' }}) }}
                                {{ form_errors(form.nombre) }}
                            </div>
                        </div>
                        <input type="submit" class="btn btn-success" value="Crear">
                        <a href="{{ path('doctorado') }}" class="btn btn-default">Lista de doctorados</a>
                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
