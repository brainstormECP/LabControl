{% extends 'layout.html.twig' %}

{% block body -%}
    <h1>Doctorado</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Especialidad</th>
                <td>{{ entity.Especialidad }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.Id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ entity.Nombre }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('doctorado') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('doctorado_edit', { 'id': entity.Id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
