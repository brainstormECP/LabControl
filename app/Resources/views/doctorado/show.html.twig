{% extends 'layout.html.twig' %}

{% block content -%}
    <h1>Doctorado</h1>

    <table class="table-condensed">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.Id }}</td>
            </tr>
            <tr>
                <th>Título</th>
                <td>{{ entity.Nombre }}</td>
            </tr>
            <tr>
                <th>Especialidad</th>
                <td>{{ entity.Especialidad }}</td>
            </tr>
        </tbody>
    </table>
    <div class="form-group">
        <a class="btn btn-default" href="{{ path('doctorado') }}">Lista de Doctorados</a>
        {{ form(delete_form) }}
    </div>
{% endblock %}
