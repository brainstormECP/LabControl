{% extends 'layout.html.twig' %}

{% block body -%}
    <h1>Doctorado list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Especialidad</th>
                <th>Id</th>
                <th>Nombre</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('doctorado_show', { 'id': entity.Id }) }}">{{ entity.Especialidad }}</a></td>
                <td>{{ entity.Id }}</td>
                <td>{{ entity.Nombre }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('doctorado_show', { 'id': entity.Id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('doctorado_edit', { 'id': entity.Id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('doctorado_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
