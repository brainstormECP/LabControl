{% extends 'layout.html.twig' %}

{% block content -%}
    <div class="col-lg-12">
        <h1>Entradas de Reactivo</h1>
        {% for Message in app.session.flashbag.get('info') %}
            <p class="alert alert-success">
                <i class="fa fa-smile-o"></i>
                {{ Message | raw }}
            </p>
        {% endfor %}
        <div class="panel panel-default">
            <div class="panel-heading"><a href="{{ path('entradareactivo_new') }}" class="btn btn-success">Nueva
                    Entrada de Reactivo</a></div>
            <div class="panel-body">
                <div class="row">
                    <div class="col-lg-12">

                        <table class="table table-striped">
                            <thead>
                            <tr>
                                <th>Fecha</th>
                                <th>Reactivo</th>
                                <th>Cantidad</th>
                                <th>Acciones</th>
                            </tr>
                            </thead>
                            <tbody>
                            {% for entity in entities %}
                                <tr>
                                    <td>{% if entity.Fecha %}{{ entity.Fecha|date('d/m/Y') }}{% endif %}</td>
                                    <td>{{ entity.Reactivo }}</td>
                                    <td>{{ entity.Cantidad }}</td>
                                    <td>
                                        <a class="btn btn-info"
                                           href="{{ path('entradareactivo_edit', { 'id': entity.Id }) }}">Editar</a>|
                                        <a class="btn btn-danger"
                                           href="{{ path('entradareactivo_show', { 'id': entity.Id }) }}">Eliminar</a>
                                    </td>
                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
