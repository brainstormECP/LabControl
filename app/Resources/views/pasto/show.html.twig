{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Pasto</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Tipoestudio</th>
                <td>{{ entity.tipoEstudio }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Noorden</th>
                <td>{{ entity.noOrden }}</td>
            </tr>
            <tr>
                <th>Claveinterna</th>
                <td>{{ entity.claveInterna }}</td>
            </tr>
            <tr>
                <th>Claveexterna</th>
                <td>{{ entity.claveExterna }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-default" href="{{ path('pasto') }}">
        Lista
    </a>
    {{ form(delete_form) }}

{% endblock %}
