{% extends 'layout.html.twig' %}

{% block aditionalStyle %}
    <link href="{{ asset('bower_components/datepicker/css/datepicker.css')}}" rel="stylesheet" type="text/css">
{% endblock %}

{% block content -%}
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading"> <h3>Editar Experimento</h3></div>
            <div class="panel-body">
                <div class="row">
                    <div class="col-lg-9">
                        {{ form_start(edit_form, {'attr':{ 'class':'form-horizontal' }}) }}
                        <div class="form-group">
                            <div class="col-md-7">
                                {{ form_label(edit_form.fechaInicio) }}
                                {{ form_widget(edit_form.fechaInicio,{'attr':{ 'class':'form-control' }}) }}
                                {{ form_errors(edit_form.fechaInicio) }}
                            </div>
                        </div>
                        <div class="form-group">
                            <div class="col-md-7">
                                {{ form_label(edit_form.fechaFin) }}
                                {{ form_widget(edit_form.fechaFin,{'attr':{ 'class':'form-control' }}) }}
                                {{ form_errors(edit_form.fechaFin) }}
                            </div>
                        </div>
                        <div class="form-group">
                            <div class="col-md-7">
                                {{ form_label(edit_form.observaciones) }}
                                {{ form_widget(edit_form.observaciones,{'attr':{ 'class':'form-control' }}) }}
                                {{ form_errors(edit_form.observaciones) }}
                            </div>
                        </div>
                        <div class="form-group">
                            <div class="col-md-7">
                                {{ form_label(edit_form.actividadCientifica,'Actividad Científica') }}
                                {{ form_widget(edit_form.actividadCientifica,{'attr':{ 'class':'form-control' }}) }}
                                {{ form_errors(edit_form.actividadCientifica) }}
                            </div>
                        </div>
                        <input type="submit" class="btn btn-success" value="Guardar">
                        <a href="{{ path('experimento') }}" class="btn btn-default">Lista de experimentos</a>
                        {{ form_end(edit_form) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block adicionalscripts %}
    <script src="{{ asset('bower_components/datepicker/js/bootstrap-datepicker.js')}}"></script>

    <script>
        $(document).ready(function(){
            $('#appbundle_experimento_fechaInicio').datepicker();
            $('#appbundle_experimento_fechaFin').datepicker();
        });
    </script>
{% endblock %}
