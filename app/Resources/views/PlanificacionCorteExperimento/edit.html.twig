{% extends 'layout.html.twig' %}

{% block aditionalStyle %}
    <link href="{{ asset('bower_components/datepicker/css/datepicker.css')}}" rel="stylesheet" type="text/css">
{% endblock %}

{% block content -%}
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading"> <h3>Editar Programacion</h3></div>
            <div class="panel-body">
                <div class="row">
                    <div class="col-lg-9">
                        {{ form_start(edit_form, {'attr':{ 'class':'form-horizontal' }}) }}
                        <div class="form-group">
                            <div class="col-md-7">
                                {{ form_label(edit_form.experimento) }}
                                {{ form_widget(edit_form.experimento,{'attr':{ 'class':'form-control' }}) }}
                                {{ form_errors(edit_form.experimento) }}
                            </div>
                        </div>
                        <div class="form-group">
                            <div class="col-md-7">
                                {{ form_label(edit_form.descripcion) }}
                                {{ form_widget(edit_form.descripcion,{'attr':{ 'class':'form-control' }}) }}
                                {{ form_errors(edit_form.descripcion) }}
                            </div>
                        </div>
                        <div class="form-group">
                            <div class="col-md-7">
                                {{ form_label(edit_form.fecha) }}
                                {{ form_widget(edit_form.fecha,{'attr':{ 'class':'form-control' }}) }}
                                {{ form_errors(edit_form.fecha) }}
                            </div>
                        </div>
                        <input type="submit" class="btn btn-success" value="Guardar">
                        <a href="{{ path('planificacioncorteexperimento') }}" class="btn btn-default">Lista de programacion</a>
                        {{ form_end(edit_form) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block adicionalscripts %}
    <script src="{{ asset('bower_components/datepicker/js/bootstrap-datepicker.js')}}"></script>

    <script>
        $(document).ready(function(){
            $('#appbundle_planificacioncorteexperimento_fecha').datepicker();
        });
    </script>
{% endblock %}
