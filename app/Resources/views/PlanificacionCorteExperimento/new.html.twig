{% extends 'layout.html.twig' %}

{% block aditionalStyle %}
    <link href="{{ asset('bower_components/datepicker/css/datepicker.css')}}" rel="stylesheet" type="text/css">
{% endblock %}

{% block content -%}
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading"> <h3>Crear planificacion</h3></div>
            <div class="panel-body">
                <div class="row">
                    <div class="col-lg-9">
                        {{ form_start(form, {'attr':{ 'class':'form-horizontal' }}) }}
                        <div class="form-group">
                            <div class="col-md-7">
                                {{ form_label(form.experimento) }}
                                {{ form_widget(form.experimento,{'attr':{ 'class':'form-control' }}) }}
                                {{ form_errors(form.experimento) }}
                            </div>
                        </div>
                        <div class="form-group">
                            <div class="col-md-7">
                                {{ form_label(form.descripcion) }}
                                {{ form_widget(form.descripcion,{'attr':{ 'class':'form-control' }}) }}
                                {{ form_errors(form.descripcion) }}
                            </div>
                        </div>
                        <div class="form-group">
                            <div class="col-md-7">
                                {{ form_label(form.fecha) }}
                                {{ form_widget(form.fecha,{'attr':{ 'class':'form-control' }}) }}
                                {{ form_errors(form.fecha) }}
                            </div>
                        </div>
                        <input type="submit" class="btn btn-success" value="Crear">
                        <a href="{{ path('planificacioncorteexperimento') }}" class="btn btn-default">Lista de planificaciones</a>
                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block adicionalscripts %}
    <script src="{{ asset('bower_components/datepicker/js/bootstrap-datepicker.js')}}"></script>

    <script>
        $(document).ready(function(){
            $('#appbundle_planificacioncorteexperimento_fecha').datepicker();
        });
    </script>
{% endblock %}
