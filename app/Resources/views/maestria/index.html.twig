{% extends 'layout.html.twig' %}

{% block body -%}
    <h1>Maestria list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Edicion</th>
                <th>Id</th>
                <th>Nombre</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('maestria_show', { 'id': entity.Id }) }}">{{ entity.Edicion }}</a></td>
                <td>{{ entity.Id }}</td>
                <td>{{ entity.Nombre }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('maestria_show', { 'id': entity.Id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('maestria_edit', { 'id': entity.Id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('maestria_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
