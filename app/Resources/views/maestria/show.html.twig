{% extends 'layout.html.twig' %}

{% block body -%}
    <h1>Maestria</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Edicion</th>
                <td>{{ entity.Edicion }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.Id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ entity.Nombre }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('maestria') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('maestria_edit', { 'id': entity.Id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
