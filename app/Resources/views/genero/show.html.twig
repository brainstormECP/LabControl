{% extends 'layout.html.twig' %}

{% block content -%}
    <h1>Género</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.Id }}</td>
        </tr>
        <tr>
            <th>Nombre</th>
            <td>{{ entity.Nombre }}</td>
        </tr>
        </tbody>
    </table>
    <div class="form-group">
        <a class="btn btn-default" href="{{ path('genero') }}">Lista de Géneros</a>
        {{ form(delete_form) }}
    </div>
{% endblock %}
