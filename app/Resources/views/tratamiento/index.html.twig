{% extends 'layout.html.twig' %}

{% block body -%}
    <h1>Tratamiento list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Descripcion</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('tratamiento_show', { 'id': entity.Id }) }}">{{ entity.Id }}</a></td>
                <td>{{ entity.Nombre }}</td>
                <td>{{ entity.Descripcion }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('tratamiento_show', { 'id': entity.Id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('tratamiento_edit', { 'id': entity.Id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('tratamiento_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
