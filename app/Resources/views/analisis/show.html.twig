{% extends 'tecnico.layout.html.twig' %}

{% block content -%}
    <h1>Analisis</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.Id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ entity.Nombre }}</td>
            </tr>
            <tr>
                <th>Formula</th>
                <td>{{ entity.Formula }}</td>
            </tr>
        </tbody>
    </table>

    <div class="form-group">
        <a class="btn btn-default" href="{{ path('analisis') }}">Lista de Análisis</a>
        {{ form(delete_form) }}
    </div>
{% endblock %}
