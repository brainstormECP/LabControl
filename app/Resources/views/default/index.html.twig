{% extends 'moderna.layout.html.twig' %}

{% block featured %}
<section id="featured">
    <!-- start slider -->
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                {% for Message in app.session.flashbag.get('info') %}
                    <p class="alert alert-success">
                        <i class="fa fa-smile-o"></i>
                        {{ Message | raw }}
                    </p>
                {% endfor %}
                <!-- Slider -->
                <div id="main-slider" class="flexslider">
                    <ul class="slides">
                        <li>
                            <img src="{{ asset('moderna/img/slides/1.jpg') }}" alt="" />
                            <div class="flex-caption">
                                <h3>Gestión de muestras y su seguimiento</h3>
                                <p>Se analizan anualmente más de 1000 muestras</p>

                            </div>
                        </li>
                        <li>
                            <img src="{{ asset('moderna/img/slides/2.jpg') }}" alt="" />
                            <div class="flex-caption">
                                <h3>Ensayos</h3>
                                <p>Actualmente se realizan siete ensayos y se garantiza la gestión de todas las etapas del proceso analítico desde la recepción de la muestra, a la emisión de informes finales</p>
                            </div>
                        </li>
                        <li>
                            <img src="{{ asset('moderna/img/slides/3.jpg') }}" alt="" />
                            <div class="flex-caption">
                                <h3>Historia</h3>
                                <p>El Laboratorio de Análisis Químico se creó en 1966 para contribuir a las investigaciones que se realizan en la EEPF IH</p>

                            </div>
                        </li>
                    </ul>
                </div>
                <!-- end slider -->
            </div>
        </div>
    </div>
</section>
{% endblock %}
