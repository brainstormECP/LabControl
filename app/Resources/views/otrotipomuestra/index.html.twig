{% extends 'layout.html.twig' %}

{% block content -%}
    <h1>Lista de Tipos de Muestra</h1>
    {% for Message in app.session.flashbag.get('info') %}
        <p class="alert alert-success">
            <i class="fa fa-smile-o"></i>
            {{ Message | raw }}
        </p>
    {% endfor %}
    <a class="btn btn-success" href="{{ path('otrotipomuestra_new') }}">Nuevo Tipo de Muestra</a>
    <table class="table">
        <thead>
        <tr>
            <th>Nombre</th>
            <th>Acciones</th>
        </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>{{ entity.Nombre }}</td>
                <td>
                    <a class="btn btn-info" href="{{ path('otrotipomuestra_edit', { 'id': entity.Id }) }}">Editar</a>
                    <a class="btn btn-danger" href="{{ path('otrotipomuestra_show', { 'id': entity.Id }) }}">Borrar</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
