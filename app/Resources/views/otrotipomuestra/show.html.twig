{% extends 'layout.html.twig' %}

{% block content -%}
    <h1>Tipo de Muestra</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.Id }}</td>
        </tr>
        <tr>
            <th>Nombre</th>
            <td>{{ entity.Nombre }}</td>
        </tr>
        </tbody>
    </table>
    <div class="form-group">
        <a class="btn btn-default" href="{{ path('otrotipomuestra') }}">Lista de Tipos de Muestra</a>
        {{ form(delete_form) }}
    </div>
{% endblock %}
